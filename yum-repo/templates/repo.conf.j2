server {
    listen 80;
    {% if yum_repo.server_name is defined %}
    server_name {{ yum_repo.server_name }};
    {% endif %}
    location ~ /\..* {
        deny all;
        log_not_found off;
    }

    location / {
        autoindex on;
        root /data/repo/;
    }
}
